CREATE TABLE INSTANT_POLL
   (SURVEY_ID                   NUMBER(18) NOT NULL,
    SUBMISSION_START_DATE       DATE,
    SUBMISSION_END_DATE         DATE,
    AUDIENCE_TYPE 			    VARCHAR2(40) NOT NULL,
    STATUS                      VARCHAR2(40) NOT NULL,
    NOTIFY_PAX                  NUMBER(1,0) DEFAULT 0,
    IS_EMAIL_ALREADY_SENT       NUMBER(1,0) DEFAULT 0,
    CONSTRAINT SURVEY_PK PRIMARY KEY (SURVEY_ID)
   ) 
/