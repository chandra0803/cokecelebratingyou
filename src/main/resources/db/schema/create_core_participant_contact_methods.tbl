CREATE TABLE participant_contact_method(
	user_id				NUMBER (18) NOT NULL,
	contact_method_code VARCHAR2 (60) NOT NULL,
	is_primary   		NUMBER (1) NOT NULL,
	created_by number(18) NOT NULL,
    date_created     	DATE NOT NULL)
/
ALTER TABLE PARTICIPANT_CONTACT_METHOD ADD CONSTRAINT PARTICIPANT_CONTACT_METHOD_FK
  FOREIGN KEY (USER_ID) REFERENCES APPLICATION_USER (USER_ID) 
/
CREATE INDEX PAX_CONTACT_METHOD_USER_FK_idx ON PARTICIPANT_CONTACT_METHOD
  (USER_ID)
/
