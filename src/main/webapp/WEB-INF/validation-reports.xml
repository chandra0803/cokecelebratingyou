<?xml version="1.0" encoding="ISO-8859-1" ?>

<!DOCTYPE form-validation PUBLIC
          "-//Apache Software Foundation//DTD Commons Validator Rules Configuration 1.1.3//EN"
          "http://jakarta.apache.org/commons/dtds/validator_1_1_3.dtd">

<form-validation>
  <formset>
<!-- 
   	<form name="reportParametersForm">
       <field property="reportType" depends="required">
         <arg0 key="report.parameters.REPORT_FIELD" />
       </field>

       <field property="fromDate" depends="required">
         <arg0 key="report.parameters.START_FIELD" />
       </field>

       <field property="toDate" depends="required">
         <arg0 key="report.parameters.END_FIELD" /> 
       </field>

	   <field property="enrollmentFromDate" depends="required">
         <arg0 key="report.parameters.ENROLLMENT_DATE_FIELD" />
       </field>

       <field property="enrollmentToDate" depends="required">
         <arg0 key="report.parameters.ENROLLMENT_DATE_FIELD" /> 
       </field>
       
    </form>
    -->
    <form name="reportDashboardForm">
       <field property="highlights" depends="required">
         <arg0 key="report.dashboard.HIGHLIGHTS" />
       </field>
    </form>

  </formset>
</form-validation>
  
  
  